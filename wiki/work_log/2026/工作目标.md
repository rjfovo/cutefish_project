需要实现的功能：
    1. 构建一个live版本的体验系统。
    2. 开发软件管理程序:
        1. 支持deb,appimage两种格式
        2. 对debian所有软件包实现可控。
    3. 开发系统任务管理器：
        1. cpu,内存,磁盘,网络,gpu实现监控。
        2. 对进程实现监控，对进程使用的系统资源进行监控(cpu,内存,磁盘,网络,gpu,线程)。
        3. 应用程序开机启动。
        4. 用户管理。
        5. systemd管理的服务实现监控
    4. 硬件管理器:
        管理系统的所接入的硬件
    5. bug修复和系统调优

-----------------------------------------------------------------------------------------

---------------------------------------------Epic----------------------------------------
功能细分：
1. 构建一个live版本的体验系统：
分析：
    1. 不仅仅是构建一个iso镜像的问题，而是要搭建一个框架可以快速的进行开发。
    2. 比如我要怎么开发调试，怎么从零快速的搭建起系统，其他人怎么搭建起一套类似的环境问题。
    3. 这样要能方便的管理系统，软件包怎么样的，我怎么进行管理，其他人怎么方便的参与进来。

要求：
    1. 实现通过配置来构建系统，Debian有一套自己构建系统的方式，我这边主要有两个要点：
        1. 通过环境配置来管理软件包，不同的环境对应不同的软件包列表
        2. 每个环境会需要不同的脚本，这些脚本会在构建前，构建时，构建后执行
    2. 搭建软件包管理
        1. Debian有自己的deb包管理，这个是我可以运用
        2. cutefish需要有自己的deb包管理，这个是我cutefish自用的软件包列表所以也是我搭建
        3. 系统软件管理程序还是需要做的我需要能搜索，安装软件包。当前有个更新，安装程序，但是功能不完善。
        4. appimage以后也是我这边要支持的，将来打算作为应用程序的格式（这个可以后面做）
        5. 应用商店，以appimage为基础（这个后面做）
    3. wiki文档系统管理
        1. 基础系统的功能管理
        2. 软件包文档
        3. 开发文档，别人能进行参考

