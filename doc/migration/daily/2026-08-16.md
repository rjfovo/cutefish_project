# 开发会话日志 2026-08-16

## 会话开始

- 完整阅读 `doc/future/` 下 8 份必读方案文档。
- 审计仓库目录、git 子模块、旧构建脚本、旧产物布局和旧 X11/SDDM/KWin 引用。
- 建立 `doc/migration/` 进度结构并记录 CONFLICT-001 ~ CONFLICT-006。

## 完成内容

1. 新构建系统 `wayland-scripts/build_code.sh`：
   - 与旧脚本相同四种使用方式；
   - `CUTEFISH_ROOT` 可覆盖根目录，无硬编码绝对路径；
   - 分类输出 build/packages/symbols/logs/cache；
   - dpkg 在源码副本执行、排除 .git、记录源 commit、设置 SOURCE_DATE_EPOCH；
   - clean 只清理新产物目录，保护 build.cache 文件和 legacy 产物。
2. `cutefish-compositor-core`：
   - VirtualBackend + 接口边界 + 生命周期状态机；
   - 双 Wayland socket（apps/shell）隔离，`cutefish_core_v1` 仅 shell socket 可见；
   - minimal xdg-shell 使 Qt Wayland 客户端可初始化；
   - systemd 草案；KMS 选项安全拒绝。
3. `cutefish-shell`：五态 QML 骨架 + Wayland 连接测试模式 + offscreen 测试。
4. `cutefish-installer`：固定流程 QML + 危险 Job 安全拒绝 + 固定策略配置。
5. `cutefish/wayland-code/core`：Wayland-clean 包边界迁移副本，deb 无 kwin-wayland/xwayland 依赖。
6. CI 检查：源码禁用符号 0 命中；新包二进制直接依赖/符号扫描通过。
7. ISO 等价包清单：target/live 清单不包含 SDDM/Calamares/Xorg/XWayland/KWin。
8. 验证 `build_code.sh all`、`clean`、`clean <project>`，legacy 产物保持不变。

## 测试命令摘要

```text
./wayland-scripts/build_code.sh all
ctest --test-dir cutefish/output/build/cutefish-compositor-core/cmake --output-on-failure
ctest --test-dir cutefish/output/build/cutefish-shell/cmake --output-on-failure
ctest --test-dir cutefish/output/build/cutefish-installer/cmake --output-on-failure
./wayland-scripts/test_stage0_integration.sh
./wayland-scripts/check_x11_symbols.sh
./wayland-scripts/check_binary_symbols.sh
./wayland-scripts/iso/check-manifests.sh
```

以上命令均通过。

## 完成位置

- stage-0 工程基线主体完成；
- 下一步：补齐 core 服务迁移副本，然后执行 stage-0 最终验收；
- stage-0 验收通过前不进入 stage-1。

## 未完成事项

- core 服务迁移副本：session/settings-daemon/powerman/notificationd/gmenuproxy/chotkeys。
- 安装器磁盘 Job 和 polkit helper（需专用硬件验证环境）。
- KMS/Libinput/Logind 真机路径（CONFLICT-006 等待硬件批准）。
- `wayland-scripts/iso/` 目前为包清单和检查器，完整 live/target 构建脚本待实现。

## 阻塞项

- 无 stage-0 阻塞；CONFLICT-006 为 stage-1 KMS Spike 前置阻塞。
