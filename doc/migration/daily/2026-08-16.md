# 开发会话日志 2026-08-16

## 会话开始

- 完整阅读 `doc/future/` 下 8 份必读方案文档。
- 审计仓库目录、git 子模块、旧构建脚本、旧产物布局和旧 X11/SDDM/KWin 引用。
- 建立 `doc/migration/` 进度结构并记录 CONFLICT-001 ~ CONFLICT-006。

## 完成内容

1. 新构建系统 `wayland-scripts/build_code.sh`：四种旧用法、CUTEFISH_ROOT 覆盖、
   分类输出、dpkg 源码副本、SOURCE_DATE_EPOCH、日志、clean 边界。
2. `cutefish-compositor-core`：VirtualBackend、双 socket、cutefish_core_v1、
   minimal xdg-shell、状态机、systemd 草案、KMS 安全拒绝。
3. `cutefish-shell`：五态 QML 骨架、Wayland 连接测试模式、offscreen 测试。
4. `cutefish-installer`：固定流程 QML、危险 Job 安全拒绝、固定策略配置。
5. `cutefish/wayland-code/core`：9 个 Wayland-clean 组件，其中 session、
   settings-daemon、powerman、notificationd 为 Wayland 原生迁移副本。
6. CI 检查：源码禁用符号 0 命中；二进制直接依赖/符号扫描通过。
7. ISO 等价包清单：target/live 清单通过自检。
8. 验证 `build_code.sh all`、`clean`、`clean <project>`，legacy 产物保持不变。

## 测试命令摘要

```text
./wayland-scripts/build_code.sh all
ctest --test-dir cutefish/output/build/cutefish-compositor-core/cmake --output-on-failure
./wayland-scripts/test_stage0_integration.sh
./wayland-scripts/check_x11_symbols.sh
./wayland-scripts/check_binary_symbols.sh
./wayland-scripts/iso/check-manifests.sh
```

全部通过。

## stage-0 验收判定

按 `stage-0-wayland-only-baseline/README.md` 第 6 节逐项核对：

1. cutefish-core 新包依赖无 kwin-wayland/xwayland、无 X11 session：通过。
2. 产品构建不含 SDDM/X11 session：新包和新 ISO 清单层面通过；LoginShell 为发布门槛已记录。
3. Calamares 配置不再引用旧显示链的最终目标：新清单已排除 Calamares/calamares-settings。
4. core 启动 Wayland server，shell 和最小客户端可连接：通过。
5. CI 阻断新增 X11/KWin 符号：通过。
6. 不宣称阶段 0 为可发布镜像：通过。

因此 stage-0 标记为验收通过。该阶段是工程基线，不是产品发布。

## 完成位置

- stage-0 验收通过；
- stage-1 未开始，前置阻塞为 CONFLICT-006（KMS 真机复验硬件批准）。

## 未完成事项

- stage-1 KMS/Libinput/Logind 真机路径。
- 安装器磁盘 Job 和 polkit helper。
- 其余 QML 迁入/重构模块。
- 完整 ISO 构建脚本。

## 阻塞项

- CONFLICT-006：stage-1 KMS Spike 需要 AMD/Intel 真机；当前测试服务器禁止打开
  /dev/dri、/dev/input，也未获得其他专用硬件环境。
