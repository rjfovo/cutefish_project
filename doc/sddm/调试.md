sudo journalctl -f | grep cutefish
使用这个命令可以实时看cutefish相关的日志

tail -f  ~/.xsession-errors
这个命令可以看cutefish-xsession进程的日志

最简单的方式调试桌面会话
export QT_DEBUG_PLUGINS=1
export QML_DEBUG=1
export QT_LOGGING_RULES="qt.qpa.*=true;qt.qml.*=true;js=true"
startx /usr/bin/cutefish-session -- :1 -keeptty 2>&1 | tee ~/cutefish_debug_full.log