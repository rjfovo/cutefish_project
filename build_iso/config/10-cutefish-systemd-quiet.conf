[Manager]
# CutefishOS: keep boot/shutdown progress out of the visible console. Unit
# status updates are disabled; systemd logs continue to go to the journal
# (and thus to files). The final kmsg fallback is filtered by kernel.printk=2.
ShowStatus=no
LogTarget=journal-or-kmsg
